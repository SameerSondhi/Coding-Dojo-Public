// Change the text on the button from "Login" to "Logout"
function displayLogout(element) {
    element.innerText = "Logout";
}

// Make the "Add Definition" button disappear after clicking it
function makeButtonDisappear(element) {
    element.remove();
}
